[metadata]
name = foundationmsms
version = 0.1.0
description = Foundation MS/MS utilities and models
license_files = LICENSE

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    numpy
    torch

[options.packages.find]
where = src
